[SERVICE]
    Flush        1
    Daemon       Off
    Log_Level    info
    Parsers_File parsers.conf

[INPUT]
    Name tail
    Path /var/log/user-service/*.log
    Parser docker
    Tag user-service.*

[OUTPUT]
    Name stdout
    Match *
