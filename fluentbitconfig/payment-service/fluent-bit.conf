[SERVICE]
    Flush        1
    Daemon       Off
    Log_Level    info
    Parsers_File parsers.conf

[INPUT]
    Name tail
    Path /var/log/payment-service/*.log
    Parser docker
    Tag payment-service.*

[OUTPUT]
    Name stdout
    Match *
